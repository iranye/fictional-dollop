name: sample-workflow
on: [push]
jobs:
  explore-first-github-workflow:
    runs-on: ubuntu-latest
    steps:
      - run: echo "auto-generated by a ${{ github.event_name }} event"
	  - run: echo "Job is running on a ${{ runner.os }} server hosted by github"
	  - run: echo "Branch-name is ${{ github.ref }} inside repo: ${{ github.repository }}."
	  - name: Check out repo code
        uses: actions/checkout@v2	  
	  - run: echo "Repo has been cloned to the runner"	  
	  - run: echo "Workflow is now ready to test your code on the runner."
	  - name: List files in the reporisotyr
	    run: |
		  ls ${{ github.workspace }}
	  - run: echo "Job status: ${{ job.status }}."
	  
